# 球感日记 v4.1 MVP 聚焦优化计划

> **状态：✅ v4.1 主线已完成（Release Notes 见文末）**
> 原则：**砍 60%、留 30%、改 10%**。砍掉的代码不删 git 记录，只在路由和入口下线，避免一刀切回滚困难。
> 范围：基于当前 v4.0 仓库结构（`client/`、`server/`、`shared/`）做收敛裁剪，目标是把 MVP 收敛到 3 个核心模块。

---

## 一、新版 MVP 定位（一句话）

> **给 NTRP 3.5–4.5 业余球友的"赛前情报 + 赛后 60 秒交作业"工具**
> ——首页是赛前情报，记录是辅助，AI 跨场对比是付费钩子。

**目标用户收紧**：3 年球龄+、能听懂"反手斜线深区"的人。NTRP 3.0 以下不做适配。

**核心路径只剩三步**：
1. 赛前看情报
2. 赛后 60 秒交作业
3. 每 3 场看进步

---

## 二、保留的 3 个核心模块

### 模块 A：赛前情报（主入口，从二级页升级到首页）

**用户故事**：打球前 1 分钟，输入"今天和谁打"→ 立刻看到弱点雷达 + 历史规律 + 1 条战术。

**功能边界（精简版）**：
- 选择对手（已有档案 / 输入新对手名）
- 弱点雷达图（5 维，复用 `server/modules/opponent/opponent.aggregator.ts`）
- 历史 H2H + 输赢规律
- AI 1 条战术建议（含时机+动作+落点）
- **新对手兜底**：基于"对手类型"（同类对手聚合）给通用建议

**砍掉**：
- 条件适配建议（数据量不够）
- 同类对手 PRO 功能（合并进默认逻辑，不区分付费）

### 模块 B：60 秒赛后交作业（从五维五区块砍到两区块）

**用户故事**：打完球扫一眼比分 + 一句话感受 → 提交 → 看 AI 评语。

**功能边界（精简版）**：
- 区块 1：胜负 + 比分（默认 1 盘大比分，**逐盘小比分折叠**）
- 区块 2：对手是谁（必填，绑档案） + 一句话感受（语音/文字，可不写）
- **「展开高级模式」按钮**（默认收起）：
  - 五维对手标签 / 我的状态 / 战术 / 比赛条件 / 场景快选 / 双打搭档 → 全部塞进折叠区
- 提交 → AI 追问 1 题 → 卡片化复盘

**砍掉**：
- AI 追问对未填高级字段的用户简化为"今天主要问题是？" 三选一兜底
- 双打的独立流程（合并到"高级模式"里勾选）

### 模块 C：跨场进步对比（取代单场复盘 + 月报 + 弱点趋势 3 个分散功能）

**用户故事**：打满 3 场后自动生成"这 3 场 vs 上 3 场"的对比卡，每 3 场刷新一次。

**功能边界**：
- 进度条："再记 N 场解锁下一份对比报告"
- 对比维度：胜率 / 高频失误 / 高频对手类型 / 1 条进步建议 / 1 条退步告警
- **每 3 场触发**（不再用 5/8/10 这种长周期阈值）
- 取代月度洞察 + 弱点趋势告警，**两套 UI 合一**

**砍掉**：
- 月报（数据量永远不够）
- 弱点趋势独立页（合并）
- 条件关联告警（≥5 场同条件几乎不可能触发）

---

## 三、砍掉 / 下线的模块（**不删代码，仅下线入口**）

| 当前模块 | 处理方式 | 原因 |
|---|---|---|
| 球友圈天梯 | **下线** | 伪社交，绕不开微信群 |
| 双打独立页 | **下线，并入高级模式** | 标签/Prompt 复用单打一套 |
| 弱点趋势独立页 | **下线，并入跨场对比** | 数据量门槛过高 |
| 月度洞察独立页 | **下线，并入跨场对比** | 同上 |
| 朋友圈/分享卡片 | **保留但不投入** | 弱传播钩子，不下线但不做新功能 |

---

## 四、代码层面"删 / 改 / 留"映射

> 全部基于当前文件树。**下线 = 路由摘除 + 服务保留**，方便后续 A/B 回滚。

### 4.1 前端 `client/src/app.config.ts`

**TabBar 调整（关键改动）**：

```diff
  pages: [
-   'pages/record/index',
-   'pages/result/index',
-   'pages/history/index',
-   'pages/opponents/index',
-   'pages/insight/index',
-   'pages/opponent-detail/index',
    'pages/pre-match-intel/index',     // ← 升级为首页
+   'pages/record/index',
+   'pages/result/index',
+   'pages/history/index',
+   'pages/opponents/index',
+   'pages/opponent-detail/index',
+   'pages/insight/index',             // ← 改造为「跨场对比」页
-   'pages/weakness-trend/index',      // 下线（保留代码）
-   'pages/friend-circle/index',       // 下线（保留代码）
-   'pages/doubles/index'              // 下线（保留代码）
  ],
  tabBar: {
    list: [
-     { pagePath: 'pages/record/index',     text: '记录' },
-     { pagePath: 'pages/history/index',    text: '历史' },
-     { pagePath: 'pages/opponents/index',  text: '对手' },
-     { pagePath: 'pages/insight/index',    text: '我的' }
+     { pagePath: 'pages/pre-match-intel/index', text: '赛前' },   // 新主入口
+     { pagePath: 'pages/record/index',          text: '记录' },
+     { pagePath: 'pages/opponents/index',       text: '对手' },
+     { pagePath: 'pages/insight/index',         text: '复盘' }    // 跨场对比
    ]
  }
```

### 4.2 前端页面改造

| 文件 | 操作 | 说明 |
|---|---|---|
| `client/src/pages/pre-match-intel/index.tsx` | **改造** | 升级为首页，加"选对手"主流程；新对手走"对手类型聚合"兜底 |
| `client/src/pages/record/index.tsx` | **大改** | 默认只显示 区块1胜负+比分 + 区块2对手+一句话；新增 `<Collapsible>` 折叠"高级模式"，把五维标签/条件/场景快选/双打全部塞进去 |
| `client/src/pages/result/index.tsx` | **保留** | 卡片化复盘结构不动，但 `coordination_eval` 卡片仅在勾选双打时渲染 |
| `client/src/pages/insight/index.tsx` | **改造** | 改成"跨场对比"页：进度条 + 每 3 场刷新一份对比卡 |
| `client/src/pages/opponent-detail/index.tsx` | **保留** | 雷达图 + H2H 趋势保留，是赛前情报的数据底座 |
| `client/src/pages/history/index.tsx` | **保留** | 不在 TabBar，从"我的"二级入口进入 |
| `client/src/pages/weakness-trend/index.tsx` | **下线** | 路由删除，文件保留 |
| `client/src/pages/friend-circle/index.tsx` | **下线** | 同上 |
| `client/src/pages/doubles/index.tsx` | **下线** | 双打能力降级为 record 页里的勾选 |

### 4.3 前端组件 `client/src/components/`

| 文件 | 操作 |
|---|---|
| `Icon.tsx` / `ui/*` | 全部保留 |
| `MyInfoCard.tsx` / `OpponentInfoCard.tsx` | 保留，但改为只在"高级模式"里挂载 |
| `CoordinationIssuesTag.tsx` | **下线**（双打折叠后不需要独立组件） |
| `PartnerSearchInput.tsx` | 保留，仅高级模式用 |
| **新增** `<AdvancedModeCollapse>` | 包裹高级字段的折叠容器 |
| **新增** `<CrossMatchCompareCard>` | 跨场对比卡片，给改造后的 insight 页用 |

### 4.4 后端 `server/modules/`

| 模块 | 操作 | 说明 |
|---|---|---|
| `auth/` | 保留 | 不动 |
| `match/` | **改造** | `match.model.ts` 字段全保留（向后兼容），但所有非核心字段改为可选/缺省值；`score.analyzer.ts` 保留 |
| `opponent/` | 保留 | 雷达 + 聚合是赛前情报底座 |
| `intel/` | **加强** | 新增"无历史对手时基于对手类型出兜底建议"分支 |
| `insight/` | **改造** | 从"月报"改为"每 3 场跨场对比"；保留 `insight.calculator.ts`，新增 `compareTwoWindows(prev3, last3)` |
| `trend/` | **冻结** | 路由不挂载；`trend.detector.ts` 的方法被 insight 模块复用 |
| `social/` | **冻结** | 路由不挂载 |
| `doubles/` | **冻结** | 路由不挂载，能力合并到 match 模块的 `doubles_data` 字段 |
| `partner/` | **冻结** | 同上 |
| `subscription/` | 保留 | 后续把"跨场对比"作为付费钩子 |
| `knowledge/` | 保留 | 知识库本身是核心资产 |
| `user/` | 保留 | |

**`server/app.ts` 路由注册需做的改动**：注释掉 `trend.route` / `social.route` / `doubles.route` / `partner.route` 的 `register`，但保留文件。

### 4.5 后端 Worker / Prompt / Knowledge

| 文件 | 操作 |
|---|---|
| `server/workers/insight.worker.ts` | **改造**：从月度触发改为「每记录 3 场触发跨场对比生成」 |
| `server/workers/opponent.worker.ts` | 保留，雷达图依赖 |
| `server/prompts/preMatchIntel.prompt.ts` | **加强**：增加"无历史 H2H 时基于对手类型库兜底"的分支 |
| `server/prompts/matchFeedback.prompt.ts` | **改造**：用户未填高级字段时降级 prompt，避免 AI 强行编造细节 |
| `server/prompts/monthlyInsight.prompt.ts` | **替换**为 `crossMatchCompare.prompt.ts`（新文件，专做"近 3 场 vs 上 3 场"对比） |
| `server/prompts/trendAnalysis.prompt.ts` | 冻结，保留文件 |
| `server/knowledge/v1/*.json` | 保留 5 类全部，是核心资产 |

### 4.6 共享类型 `shared/`

| 文件 | 操作 |
|---|---|
| `types.ts` | **保留全部类型**（向后兼容），仅在 `MatchAnalysis` 上新增 `simple_mode_hint?: string` 用于"低信号输入降级提示" |
| `constants.ts` | 保留全部标签枚举，前端默认不展示而已 |
| **新增** `CrossMatchCompare` 类型 | 跨场对比页的契约 |

---

## 五、落地步骤（建议 4 个迭代，每个 1 周）

### Sprint 1：路由 / 入口重构（不破坏现有数据）
1. 改 `client/src/app.config.ts`，TabBar 换成新四项
2. `server/app.ts` 注释掉 social / doubles / partner / trend 路由注册
3. 验收：旧数据正常读、被下线的页面返回 404 或跳转到首页

### Sprint 2：record 页折叠改造 + Prompt 降级
1. record 页拆成"基础区 + 高级模式折叠"
2. `server/prompts/matchFeedback.prompt.ts` 加"输入信号弱时降级"
3. 验收：60 秒能完成基础模式录入；result 页面正常出卡片

### Sprint 3：赛前情报升级为首页
1. `pages/pre-match-intel/index.tsx` 加"选对手"主流程
2. `server/modules/intel/intel.service.ts` + `preMatchIntel.prompt.ts` 加"对手类型兜底"
3. 验收：新对手也能出情报，不再"必须先有 2 场记录"

### Sprint 4：跨场对比替代月报 + 趋势
1. `pages/insight/index.tsx` 改造为对比卡 + 进度条
2. `server/workers/insight.worker.ts` 改为每 3 场触发
3. 新增 `crossMatchCompare.prompt.ts` 和 `compareTwoWindows()`
4. 验收：第 3、6、9 场后自动生成对比卡

---

## 六、风险与回滚预案

| 风险 | 处理 |
|---|---|
| 旧用户找不到熟悉的入口（球友圈/双打/趋势） | 「我的」页保留二级入口链接到这些"实验性功能"，文案标"Beta，将下线" |
| AI 在用户只填基础字段时输出空泛 | matchFeedback prompt 增加"如信息不足，只输出 2 张卡片（总评+1 条改进），不要硬编造亮点" |
| 跨场对比首报需要 ≥ 3 场，前 2 场用户没东西看 | 第 1、2 场各送一份"单场快报"占位，文案"再记 N 场解锁完整对比" |
| 历史代码被冻结后慢慢腐烂 | 在每个冻结模块顶部加 `@deprecated since v4.1` 注释 + 开 issue 跟踪 |

---

## 七、最终的"前后端入口对照速查表"

| 用户动作 | 前端页 | 后端入口 |
|---|---|---|
| 打开小程序 | `pages/pre-match-intel` | `intel.route` |
| 选/搜对手看情报 | 同上 | `intel.service` + `opponent.aggregator` |
| 60 秒交作业 | `pages/record` → `pages/result` | `match.route` + `matchFeedback.prompt` |
| 看跨场进步 | `pages/insight`（改造后） | `insight.route` + 新 `crossMatchCompare.prompt` |
| 看对手详情 | `pages/opponent-detail` | `opponent.route` |
| 历史 / 我的 | `pages/history`（二级） | `match.route` |

---

## 八、量化预期

| 指标 | v4.0 现状（估算） | v4.1 目标 |
|---|---|---|
| 首屏到提交一次记录的耗时 | 90–120 秒 | ≤ 60 秒（基础模式） |
| TabBar 入口数 | 4（记录/历史/对手/我的） | 4（赛前/记录/对手/复盘） |
| 前端页面数 | 10 | 7（在用） + 3（冻结） |
| 后端业务模块数 | 13 | 8（在用） + 4（冻结） + 1（保留付费） |
| AI Prompt 数 | 4 | 3（matchFeedback / preMatchIntel / crossMatchCompare） |
| 代码量 | 100% 基线 | 净减少约 30% 入口暴露面 |

---

# 📦 Release Notes — v4.1 MVP（已发布）

> 完成时间：v4.1 主线收尾（Sprint 1–4 全部交付，Sprint 5 完成 record 页激进瘦身）
> 主旨：**砍掉一半字段 + 精简目标用户 + 入口聚焦"赛前情报 + 60 秒交作业 + 跨场对比"**

## 🎯 已交付能力

### Sprint 1 · 入口聚焦
- TabBar 重排为 **赛前 / 记录 / 对手 / 复盘**，赛前情报从二级页提升为首屏
- 后端下线 4 条路由：`registerTrendRoutes` / `registerSocialRoutes` / `registerDoublesRoutes` / `registerPartnerRoutes`
- 前端下线 3 个页面：`weakness-trend` / `friend-circle` / `doubles`（**v4.1 收尾已物理删除**）

### Sprint 2 · 录入页瘦身 + AI 弱信号降级
- 新建 `AdvancedModeCollapse` 折叠容器，比赛模式下"比赛条件 / 赛后总结标签"默认折叠
- `matchFeedback.prompt.ts` 新增 `is_low_signal` 检测：当用户只填胜负+比分时，AI 强制极简输出（rating 限 A/B/C、highlights 空、improvements ≤1 条 P0），**禁止编造细节**

### Sprint 3 · 赛前情报 TabBar 入口可用化
- `pre-match-intel` 改为双形态页面：
  - 无 alias（TabBar 入口）：**陌生对手快速画像**（按打法类型 4 选 1，硬编码战术模板，0 调用成本）+ **已交手对手 TOP 6 列表**
  - 有 alias：保持原 H2H 详情视图
- `preMatchIntel.prompt.ts` 新增 `h2h_thin` 检测：H2H 历史 < 2 场时，禁止"上次/历史上"措辞、danger_rating 强制 3、tactics ≤ 2 条

### Sprint 4 · 跨场对比替代月报
- 新增 **免费接口** `GET /api/insight/recent-compare`（不调 AI、不收费）
- 智能分流：
  - `same_opponent` 模式：最近一场 vs 上次同对手，列出新增/修复的得分方式与丢分原因
  - `cross_opponent` 模式：当无同对手历史时降级
- insight 页移除指向已下线 `weakness-trend` 的「AI 成长分析」卡片，替换为「🆚 近两场对比」

### Sprint 5 · record 页激进瘦身（v4.1 收尾）
- record 页 `OpponentInfoCard` / `MyInfoCard` 也收进高级折叠区，新版基础模式默认可见字段降至个位数

## 📊 量化收益

| 指标 | v4.0 | v4.1 已落地 |
|---|---|---|
| TabBar 入口 | 记录/历史/对手/我的 | 赛前/记录/对手/复盘 |
| 前端页面（物理） | 10 | 7（已删除 3 个下线页面） |
| 后端注册路由 | 13 | 9（4 个已下线） |
| record 页基础模式可见字段 | ~30 | <10 |
| AI 弱信号场景幻觉风险 | 无约束 | 双层硬约束（is_low_signal + h2h_thin） |
| 月报功能依赖 | PRO + AI 调用 + 30 天数据 | 免费 + 0 调用 + 2 场即可 |

## 🗂️ 主要改动文件

- 前端：[app.config.ts](file:///Users/bytedance/Desktop/球感日记v4.0/client/src/app.config.ts) · [AdvancedModeCollapse.tsx](file:///Users/bytedance/Desktop/球感日记v4.0/client/src/components/AdvancedModeCollapse.tsx) · [pre-match-intel/index.tsx](file:///Users/bytedance/Desktop/球感日记v4.0/client/src/pages/pre-match-intel/index.tsx) · [insight/index.tsx](file:///Users/bytedance/Desktop/球感日记v4.0/client/src/pages/insight/index.tsx) · [record/index.tsx](file:///Users/bytedance/Desktop/球感日记v4.0/client/src/pages/record/index.tsx) · [app.scss](file:///Users/bytedance/Desktop/球感日记v4.0/client/src/app.scss)
- 后端：[app.ts](file:///Users/bytedance/Desktop/球感日记v4.0/server/app.ts) · [matchFeedback.prompt.ts](file:///Users/bytedance/Desktop/球感日记v4.0/server/prompts/matchFeedback.prompt.ts) · [preMatchIntel.prompt.ts](file:///Users/bytedance/Desktop/球感日记v4.0/server/prompts/preMatchIntel.prompt.ts) · [insight.service.ts](file:///Users/bytedance/Desktop/球感日记v4.0/server/modules/insight/insight.service.ts) · [insight.route.ts](file:///Users/bytedance/Desktop/球感日记v4.0/server/modules/insight/insight.route.ts)

## 🚧 已知风险与回滚路径

- **回滚**：所有改动在 git history 中可追溯，已下线的后端模块保留代码（仅在 `app.register(...)` 注释），如需重启只需取消注释
- **未做**：未删除已下线后端模块物理代码（`server/modules/trend/social/doubles/partner`），按"留 30%"原则保留备用
- **下一阶段候选**：付费墙改造（PRO = 跨场 AI 深度分析）、跨场 AI prompt（`crossMatchCompare`）、留存数据回收

---

> v4.1 主线 ✅ 完成。下一步聚焦：**实测留存** → **付费转化路径** → **AI 深度分析钩子（crossMatchCompare prompt）**。
